# filepath: [pubspec.yaml](http://_vscodecontentref_/1)
name: offline_image_upload
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  image_picker: ^1.0.7
  connectivity_plus: ^5.0.2
  http: ^1.2.1
  google_fonts: ^6.2.1
  flutter_local_notifications: ^17.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true